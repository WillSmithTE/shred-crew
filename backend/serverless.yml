org: willsmithte
app: aws-node-express-dynamodb-api
service: aws-node-express-dynamodb-api
frameworkVersion: '3'

custom:
  usersTableName: 'shred-crew-users-${sls:stage}'
  resortsTableName: 'shred-crew-resorts-${sls:stage}'
  dynamodb:
    start:
      migrate: true
    stages:
      - dev

provider:
  name: aws
  region: eu-central-1
  runtime: nodejs14.x
  iam:
    role:
      statements:
        - Effect: Allow
          Action:
            - dynamodb:Query
            - dynamodb:Scan
            - dynamodb:GetItem
            - dynamodb:PutItem
            - dynamodb:UpdateItem
            - dynamodb:DeleteItem
          Resource:
            - !Sub '${ResortsTable.Arn}/*'
            - !Sub '${UsersTable.Arn}/*'
            - Fn::GetAtt: [ UsersTable, Arn ]
            - Fn::GetAtt: [ ResortsTable, Arn ]
  environment:
    USERS_TABLE: ${self:custom.usersTableName}
    RESORTS_TABLE: ${self:custom.resortsTableName}

functions:
  api:
    handler: dist/handler.handler
    events:
      - httpApi: '*'

resources:
  - ${file(db/users.yml)}
  - ${file(db/resorts.yml)}

plugins:
  - serverless-dynamodb-local
  - serverless-offline
  - serverless-dynamodb-seed

package:
  patterns:
    - '!.dynamodb/**'
    - '!./db/bootstrap/resorts.json'
